# `factors/moutai/quantitative/` — 茅台定量因子

本目录为 **因子时间序列**（如 `moutai_fundamental_quant_quarterly.csv`），供估值、回测等读取。

**PE 等估值产出与说明**已迁至与 `data/`、`factors/` 并列的 **`valuations/moutai/pe/`**（配置 `config.json`、结果 `intrinsic_quarterly.csv`、说明 `README.md`）。见仓库根目录 **`valuations/README.md`**。
